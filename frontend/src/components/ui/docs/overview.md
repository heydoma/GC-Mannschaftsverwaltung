# frontend/src/components/ui Overview

## Zweck
Reusable UI-Komponenten.

## Komponenten
- `button`, `card`, `input`, `select`, `table`, `dialog`, `badge`, `sonner`.

## Nutzung
- Imports ueber `@/components/ui/...`.
- Styles kommen aus shadcn und globalem Theme.
