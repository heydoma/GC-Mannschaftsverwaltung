# --- PostgreSQL (Docker) ---
POSTGRES_USER=golf
POSTGRES_PASSWORD=golf_dev_pw
POSTGRES_DB=golf_team
POSTGRES_PORT=5432

# --- Adminer (Docker) ---
ADMINER_PORT=8080

# --- Backend (FastAPI, lokal) ---
DATABASE_URL=postgresql://golf:golf_dev_pw@localhost:5432/golf_team
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# --- Keycloak (Docker) ---
KEYCLOAK_PORT=8180
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# --- Backend: Keycloak Integration ---
KEYCLOAK_URL=http://localhost:8180
KEYCLOAK_REALM=golf-team-manager
KEYCLOAK_BACKEND_CLIENT_ID=golf-backend
KEYCLOAK_BACKEND_CLIENT_SECRET=replace-me-after-realm-import

# --- Frontend (Vite, lokal) ---
VITE_API_URL=http://localhost:8000/api
VITE_KEYCLOAK_URL=http://localhost:8180
VITE_KEYCLOAK_REALM=golf-team-manager
VITE_KEYCLOAK_CLIENT_ID=golf-frontend
